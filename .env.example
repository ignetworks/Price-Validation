NS_ACCOUNT_ID=5192615     # NetSuite account ID
NS_CONSUMER_KEY=          # OAuth consumer key (from Integration Record)
NS_CONSUMER_SECRET=       # OAuth consumer secret (from Integration Record)
NS_TOKEN_ID=              # Access token ID (from Access Token setup)
NS_TOKEN_SECRET=          # Access token secret (from Access Token setup)
POLL_INTERVAL=30          # Minutes between poll cycles (default 30)
